<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.test_01">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_foreground"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@drawable/ic_launcher_foreground"
        android:supportsRtl="true"
        android:theme="@style/Theme.Test_01">
        <activity
            android:name=".activities.FullscreenImageActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_fullscreen_image"
            android:theme="@style/Theme.Test_01.Fullscreen" />
        <activity
            android:name=".activities.AlbumGridActivity"
            android:configChanges="orientation|screenSize" /> <!-- <activity -->
        <!-- android:name=".activities.PictureGridActivity" -->
        <!-- android:configChanges="orientation|screenSize" -->
        <!-- android:label="@string/title_activity_picture_grid" -->
        <!-- android:theme="@style/Theme.Test_01.NoActionBar" /> -->
        <activity
            android:name=".activities.ImageGridActivity"
            android:configChanges="orientation|screenSize" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>